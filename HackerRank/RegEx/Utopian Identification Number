# Solution of Regex challenge on HackerRank
# Utopian Identification Number
# Found at https://www.hackerrank.com/challenges/utopian-identification-number/problem?isFullScreen=true
# --------------------------------------------------------------

from re import match

pattern = "[a-z]{0,3}\d{2,8}[A-Z]{3,}"

for _ in range(int(input())):
    print("VALID" if match(pattern, input()) else "INVALID")